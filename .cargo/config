[http]
proxy = "127.0.0.1:3128"       # HTTP proxy to use for HTTP requests (defaults to none)
timeout = 60000     # Timeout for each HTTP request, in milliseconds

[https]
proxy = "127.0.0.1:3128"      # HTTP proxy to use for HTTP requests (defaults to none)